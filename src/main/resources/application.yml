org:
  quartz:
    job-store-type: memory
    scheduler-name: simple-scheduler
    jobStore:
      class=org:
        quartz:
          simpl: RAMJobStore

schedule:
  jobs:
    hello:
#      cron: "0/4 * * * * ?"
      cron: "-"
    bye:
      cron: "0/5 * * * * ?"