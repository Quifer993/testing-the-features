Параметры запуска:
java target/quartz-test-1.0-SNAPSHOT.jar --spring.config.additional-location=target/classes/quartz-datasource-local.yml

В самом файле quartz-datasource-local.yml настройте нужную базу и login/password

Если запускается более 1 инстанса, то каждый раз базу перезаписывать заново опасно. Поэтому флаг 'spring.quartz.jdbc.initialize-schema' выставить после первого запуска -> never

autoopertaion - job
scheduler -	jobDetails
конфиг 	  -	triggers (отдельно создать класс, который будет проходить по всем джобам)
